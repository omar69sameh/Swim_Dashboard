# Migrations

Run each `.sql` file once in **Supabase Dashboard → SQL Editor** for project `imu_reader`.

Required before coach dropdown and assignments work:

- `001_profiles_role_coach.sql`

Required before ML analysis and quality scores on the dashboard:

- `002_session_analysis.sql`
